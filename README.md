# AI CHOCH Render Deployment

## 1️⃣ GitHub'a Push Et
```bash
git init
git add .
git commit -m "Initial commit AI CHOCH Render"
git branch -M main
git remote add origin <GITHUB_REPO_URL>
git push -u origin main
